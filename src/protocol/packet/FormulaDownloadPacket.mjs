import Packet from '../Packet.mjs';

export default class FormulaDownloadPacket extends Packet {
    static id = 0;

    constructor(wbuf) {
        super(wbuf);
    }

    decode() {

    }

    encode() {

    }
}