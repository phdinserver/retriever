import Packet from '../Packet.mjs';

export default class SchoolMealRetrievePacket extends Packet {
    static id = 1;

    constructor(wbuf) {
        super(wbuf);
    }

    decode() {

    }

    encode() {

    }
}